{
  "name": "forgeroom",
  "version": "0.2.0",
  "private": true,
  "description": "Adaptive GStack plus Grill Me idea refinement app.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check api/coach.js && node --check server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
